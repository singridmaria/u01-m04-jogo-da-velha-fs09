//Variáveis

var gasolina = parseFloat(prompt(3))

var alcool = parseFloat(prompt(4))

var div = alcool / gasolina;

//Processamento e Saída

if(div > 0.7 ) {

alert ('Vale a pena a Alcool') }

else if (div < 0.7) {

alert ('Vale a pena o Gasolina')}

else{

alert ('tanto Faz Abastecer um como o outro')

var start = parseFloat(prompt("deseja iniciar novamente? 'Sim(S) ou Não(N)'"))}